# @vercel/analytics and @vercel/speed-insights' Vue integration hasn't
# declared vue-router v5 support yet (peerOptional "^4"), even though it
# works fine with the v5 Nuxt 4 actually installs. Without this, a clean
# `npm install` (e.g. on Vercel) fails with ERESOLVE.
legacy-peer-deps=true
